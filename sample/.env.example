# Debug-only DesiPass validation. Copy values to an ignored .env.local file,
# or point desipass.envFile in the repository's ignored local.properties to an
# existing Flutter/React Native demo environment file.
DESIPASS_GRAPHQL_URL=
DESIPASS_API_KEY=
